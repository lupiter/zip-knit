[project]
name = "zip-knit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pillow>=11.1.0",
    "pyserial>=3.5",
]

[dependency-groups]
lint = [
    "black>=25.1.0",
    "pylint>=3.3.4",
    "ruff>=0.9.6",
]
test = [
    "pytest>=8.3.4",
]
